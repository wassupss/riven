node_modules
out
dist
release
.vite
.DS_Store
*.log
*.tsbuildinfo

# native (Swift) app build artifacts
.build/
DerivedData/
*.xcodeproj
riven.app/
# libghostty framework — provisioned separately (prebuilt asset), not committed
ghostty-fw

# local landing-page preview asset (ship the dmg via a real host / CDN instead)
landing/*.dmg

# env / secrets
.env
.env.*
!.env.example
!.env.build.example
*.local
*.pem
*.key

# Sub-agent worktrees (transient)
.claude/worktrees/
